import { TemplDirective } from './templ.directive';

describe('TemplDirective', () => {
  it('should create an instance', () => {
    const directive = new TemplDirective();
    expect(directive).toBeTruthy();
  });
});
